# FocusVeil 更新说明

## v0.1.5

发布日期：2026 年 7 月 30 日

本版本修复了关闭前台应用窗口后遮罩状态异常的问题。当某个应用有多个窗口时，关闭其中一个窗口后，FocusVeil 会继续以该应用剩余窗口作为当前焦点；当这个应用的普通窗口全部关闭时，FocusVeil 会在同一显示器上选择当前最靠前的可见窗口接管焦点，而不是直接隐藏所有遮罩。

这一调整让焦点规则更接近 macOS 用户的视觉预期。用户关闭窗口后，注意力通常会落到下方自然露出的窗口；FocusVeil 现在也按这个可见层级更新遮罩边界。Finder 桌面和显示桌面状态仍保持特殊处理，避免系统桌面动画过程中误把临时移出屏幕的窗口当作有效焦点。

## v0.1.4

发布日期：2026 年 7 月 29 日

本版本让不同显示器拥有独立的当前窗口。外接显示器播放视频时，即使内置显示器切到微信或其他应用输入内容，外接显示器的视频窗口仍会保持原有亮度；内置显示器则按自己的当前窗口更新压暗层级。

分级亮度也改为按显示器分别计算。应用会根据窗口位置判断它主要属于哪一个显示器，并在应用启动或显示器变更后，用每个显示器当前最上层的可见窗口补足初始状态。

## v0.1.3

发布日期：2026 年 7 月 29 日

本版本增强了分级亮度的层级差异。默认背景压暗强度提高到百分之五十二，分级亮度的默认值不再均匀分布，而是让第二级及更低层级更快接近背景压暗层，减少历史窗口看起来仍像当前活动窗口的问题。

渲染时也会对历史层级的保留亮度应用视觉响应曲线，使较低亮度设置对应更明显的压暗效果。若之前已经手动调整过分级亮度，可以在菜单中点击恢复默认来使用新的默认分布。

## v0.1.2

发布日期：2026 年 7 月 29 日

本版本修复了启用 macOS 点按墙纸以显示桌面后，点击桌面会导致所有窗口弹开，再次点击后 FocusVeil 遮罩功能消失的问题。问题出现在系统显示桌面的动画过程中，部分应用窗口会暂时移出屏幕，但仍可能被窗口列表报告为可见窗口。FocusVeil 之前会在这个过渡状态里清理活动窗口记录，导致桌面状态结束后无法把遮罩重新放回正确的窗口层级。

修复后，FocusVeil 会识别 Finder 桌面获得焦点但没有普通窗口的状态。在这个状态下，应用会临时隐藏遮罩，并保留最近活动窗口记录；当窗口重新回到屏幕可见区域后，遮罩会恢复到正确位置。遮罩窗口也改为瞬时辅助窗口行为，避免被系统显示桌面机制当作固定桌面层处理。

本版本还改进了窗口可见性判断。窗口必须有足够面积位于当前显示器范围内，才会被视为可用于恢复遮罩层级的普通窗口。这样可以避免显示桌面动画期间，正在移出屏幕的窗口被误判为有效活动窗口。

验证方式：

1. 使用 `zsh Scripts/build.sh` 完成构建。
2. 启用系统设置中的点按墙纸以显示桌面功能。
3. 打开多个应用窗口并启用 FocusVeil。
4. 点击桌面使窗口散开，再次点击恢复窗口。
5. 确认 FocusVeil 遮罩恢复正常，当前活动窗口保持原有亮度，其他窗口与桌面背景继续被压暗。
