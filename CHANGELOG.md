# FocusVeil 发布说明

本文件记录每个版本中影响用户体验、系统兼容性和发布交付的变更。面向 GitHub Releases 时，可直接使用对应版本的小节作为发布说明。

## v0.1.12

发布日期：2026 年 8 月 20 日

### 发布摘要

新增独立设置面板，并精简状态栏菜单。FocusVeil 现在将焦点开关和背景压暗强度保留在状态栏菜单中，其余偏好项集中到设置面板，减少菜单层级和高频操作时的视觉负担。本版本同时优化截图工具交互，降低从状态栏进入截图流程时的短暂停顿，并改善 PixPin 对窗口边界和压暗遮罩的识别一致性。

### 用户影响

用户点击状态栏图标后，可以直接启用或关闭背景压暗，并调整背景压暗强度。分级亮度、历史高亮窗口数量、各级亮度、辅助功能权限、登录时启动、检查更新和关于信息移入设置面板。状态栏菜单新增打开设置入口，并支持通过 Command 加逗号打开设置面板。

在截图场景中，状态栏菜单打开时 FocusVeil 会临时隐藏遮罩，减少截图工具启动阶段与遮罩刷新同时发生造成的卡顿。PixPin 识别窗口边界时，FocusVeil 的遮罩面板不再作为辅助功能元素参与窗口识别，同时保留系统截图共享能力，使 PixPin 截图预览和最终截图能够包含当前可见的压暗效果。

### 技术说明

应用新增惰性创建的 AppKit 设置窗口，窗口在首次打开时居中，后续打开保留用户移动后的位置。设置面板中的控件与现有偏好存储和焦点控制器复用同一套状态更新逻辑，分级亮度关闭时会同步禁用历史窗口数量、亮度滑杆和恢复默认按钮。检查更新按钮继续调用 Sparkle 更新控制器，并根据当前更新能力自动切换可用状态。

焦点控制器新增界面临时挂起状态，状态栏菜单打开期间会隐藏遮罩，菜单关闭后恢复原先状态。遮罩面板保持非激活、忽略鼠标事件和跨空间显示特性，并声明不作为辅助功能元素，从而减少第三方截图工具将遮罩面板纳入窗口候选集合的概率。遮罩面板仍允许系统截图共享，避免截图工具采集结果缺少压暗层。

### 验证建议

- 打开状态栏菜单，确认菜单中只保留启用背景压暗、背景压暗强度、打开设置和退出入口。
- 打开设置面板，确认分级亮度、历史高亮窗口数量、各级亮度、恢复默认、辅助功能权限、登录时启动、检查更新和关于信息均可访问。
- 切换分级亮度开关，确认相关控件的启用状态同步变化，焦点压暗效果仍即时生效。
- 关闭并重新打开设置面板，确认控件状态与当前偏好保持一致。
- 在状态栏菜单打开后启动截图，确认截图工具进入选择状态时没有明显停顿。
- 开启 PixPin 截图，确认窗口吸附仍能识别非当前最活跃窗口，截图预览和最终截图包含可见的压暗效果。

## v0.1.11

发布日期：2026 年 8 月 8 日

### 发布摘要

新增基于 Sparkle 的自动更新能力。FocusVeil 现在可以从 GitHub Releases 托管的 appcast 获取新版本信息，并在应用内完成更新包下载、签名校验、安装和重启。

### 用户影响

用户不再需要手动卸载旧版本或重新下载 DMG 覆盖安装。菜单栏中的 `检查更新` 会打开 Sparkle 更新流程；应用也会按一天一次的间隔自动检查新版本。发现可用更新后，用户可以在应用内确认安装。

### 技术说明

项目引入 Sparkle 2.9.5 作为嵌入式更新框架。应用包新增 Sparkle 框架依赖，`Info.plist` 增加更新源、公钥、自动检查和自动下载配置。构建脚本会在应用包中嵌入 Sparkle 框架并完成本地签名。发布脚本会生成带 EdDSA 签名的 `appcast.xml`，更新源指向 GitHub Releases 的最新 appcast 资产。

### 验证建议

- 运行 `zsh Scripts/release.sh`，确认 zip、DMG、appcast 和发布说明均能生成。
- 打开生成的应用，确认菜单栏 `检查更新` 能调用 Sparkle 更新窗口。
- 发布 GitHub Release 后，确认 `appcast.xml` 可通过 GitHub Releases 的 latest 下载地址访问。
- 使用低版本应用检查更新，确认应用能识别、下载并校验新版本更新包。

## v0.1.10

发布日期：2026 年 8 月 8 日

### 发布摘要

进一步修复 Chrome 全屏时仍被遮罩压暗的问题。FocusVeil 现在会识别前台应用中几乎铺满显示器的全屏候选窗口，即使该窗口没有稳定返回标准全屏属性，仍会在对应显示器上隐藏遮罩。

### 用户影响

Chrome 进入全屏空间或网页全屏后，窗口不应再被压暗。普通窗口场景继续保持原有焦点压暗效果，全屏窗口所在显示器会避免绘制遮罩，防止 macOS 全屏空间将辅助面板提升到内容窗口上方。

### 技术说明

前台应用窗口扫描不再只限于普通窗口层级。对于横向铺满显示器、面积占比足够高并贴近屏幕垂直边缘的窗口，应用会将其视为全屏候选窗口，并优先用于焦点记录和遮罩隐藏判断。该逻辑覆盖 Chrome 可能产生的独立全屏内容窗口，也降低辅助功能焦点仍停留在原浏览器窗口时的误判概率。

### 验证建议

- 将 Chrome 切换到 macOS 全屏空间，确认窗口保持原有亮度。
- 在 Chrome 中进入网页全屏，确认内容区域不会被遮罩压暗。
- 将鼠标移至屏幕顶部显示菜单栏，再返回 Chrome 窗口，确认亮度状态保持稳定。
- 回到普通窗口场景，确认当前窗口保持清晰，其他窗口和桌面背景继续被压暗。

## v0.1.9

发布日期：2026 年 8 月 8 日

### 发布摘要

修复全屏空间中菜单栏或系统界面短暂获得焦点后，原全屏窗口被持续压暗的问题。FocusVeil 现在会区分用户内容窗口与系统界面，并保留已识别的全屏窗口亮度状态。

### 用户影响

全屏窗口中将鼠标移至屏幕顶部显示菜单栏后，窗口会继续保持原有亮度，鼠标返回窗口区域后不会停留在变暗状态。Finder 全屏窗口也会被正确识别，不再因为全屏空间的层级变化而被遮罩压暗。

### 技术说明

窗口快照增加了辅助功能接口中的全屏状态读取，并在焦点刷新过程中排除菜单栏、控制中心、Dock、通知中心和其他系统界面。全屏窗口的识别不再只依赖窗口边界是否覆盖显示器，还会结合辅助功能窗口属性和窗口重叠面积，降低菜单栏显示时的尺寸变化误判。

### 验证建议

- 将浏览器、编辑器或其他普通应用切换到全屏模式，移动鼠标至屏幕顶部显示菜单栏，确认全屏窗口保持原有亮度。
- 鼠标离开菜单栏并返回全屏窗口，确认窗口不会保持变暗状态。
- 将 Finder 窗口切换到全屏模式，确认 Finder 窗口保持清晰。
- 回到普通桌面空间，确认当前窗口仍保持清晰，其他窗口和桌面背景继续被压暗。

## v0.1.8

发布日期：2026 年 8 月 6 日

### 发布摘要

修复全屏窗口进入独立空间后被遮罩压暗的问题。FocusVeil 现在会识别覆盖整块显示器的当前窗口，并在对应显示器上暂停绘制压暗层，使全屏窗口保持原有亮度。

### 用户影响

全屏浏览器、播放器、演示文稿、编辑器和其他全屏应用不再因为遮罩层级变化而变暗。普通窗口、多显示器焦点记录和分级亮度功能继续沿用原有行为。

### 技术说明

遮罩应用逻辑增加了显示器覆盖判断。当当前显示器的焦点窗口几乎覆盖整块屏幕时，对应显示器的遮罩层会隐藏，避免 macOS 全屏空间将辅助面板提升到全屏窗口上方造成视觉遮挡。

### 验证建议

- 将任一普通窗口切换到全屏模式，确认全屏窗口保持原有亮度。
- 回到普通桌面空间，确认当前窗口保持清晰，其他窗口和桌面背景继续被压暗。
- 在多显示器环境中分别切换普通窗口和全屏窗口，确认各显示器的遮罩状态互不干扰。

## v0.1.7

发布日期：2026 年 7 月 30 日

### 发布摘要

新增菜单栏检查更新功能。FocusVeil 现在可以读取 GitHub Releases 最新版本信息，并在发现新版本时提示用户打开发布页下载新版 DMG。

### 用户影响

用户不需要手动前往仓库确认版本。发布新版本后，用户可以在菜单中选择 `检查更新`，如果 GitHub 上已有更高版本，应用会显示当前版本、最新版本和下载入口。

### 技术说明

应用通过 GitHub 最新 Release API 获取 `tag_name` 和发布页地址，再与本地 `CFBundleShortVersionString` 进行版本号比较。该功能只负责发现新版本并打开下载页，不会自动替换本地应用。应用版本号更新为 0.1.7，构建号更新为 8。

### 验证建议

- 打开 FocusVeil 菜单，点击 `检查更新`，确认菜单会进入正在检查状态。
- 当 GitHub 最新版本高于本地版本时，确认应用提示发现新版本并可打开发布页。
- 当本地版本不低于 GitHub 最新版本时，确认应用提示已是最新版本。

## v0.1.6

发布日期：2026 年 7 月 30 日

### 发布摘要

新增 DMG 安装镜像作为正式发布产物。构建脚本现在会同时生成 `FocusVeil.zip` 和 `FocusVeil.dmg`，发布页可直接提供 DMG 下载。

### 用户影响

用户可以下载 `FocusVeil.dmg`，打开后将 `FocusVeil.app` 拖入应用程序文件夹，安装流程更接近 macOS 桌面应用的常规体验。

### 技术说明

DMG 使用 `hdiutil` 从临时安装目录生成，镜像中包含应用包和 `Applications` 快捷入口。应用版本号更新为 0.1.6，构建号更新为 7。

### 验证建议

- 运行 `zsh Scripts/build.sh`，确认 `outputs/FocusVeil.dmg` 和 `outputs/FocusVeil.zip` 都能生成。
- 挂载 DMG，确认其中包含 `FocusVeil.app` 和 `Applications`。
- 从 DMG 复制应用后启动，确认菜单栏应用可以正常运行并请求辅助功能权限。

## v0.1.5

发布日期：2026 年 7 月 30 日

### 发布摘要

修复关闭前台应用窗口后的焦点接管逻辑。当当前应用仍有其他普通窗口时，FocusVeil 会继续跟随该应用剩余窗口；当该应用窗口全部关闭时，应用会在同一显示器上选择最靠前的可见窗口作为新的焦点边界。

### 用户影响

关闭窗口后的遮罩状态更符合 macOS 的视觉层级。用户不需要手动重新切换应用，FocusVeil 会让自然露出的窗口继续保持清晰，避免遮罩异常隐藏或失去焦点。

### 技术说明

本版本保留 Finder 桌面和显示桌面状态的特殊处理，并加强窗口可见性判断，避免系统动画期间的临时窗口位置影响焦点历史。

### 验证建议

- 打开同一应用的多个普通窗口，关闭当前窗口，确认遮罩跟随剩余窗口。
- 关闭当前应用的所有普通窗口，确认同一显示器上方可见窗口接管焦点。
- 使用显示桌面功能往返切换，确认遮罩可恢复。

## v0.1.4

发布日期：2026 年 7 月 29 日

### 发布摘要

新增多显示器独立焦点逻辑。不同显示器会分别记录自己的当前窗口，焦点在其中一块屏幕上变化时，不会重置其他屏幕已经保持的窗口状态。

### 用户影响

外接显示器播放视频、文档或会议画面时，用户可以在内置显示器切换到聊天、浏览器或编辑器，而外接显示器上的主要窗口仍保持清晰。分级亮度也会按显示器分别计算，减少跨屏切换带来的误判。

### 技术说明

应用会根据窗口与显示器的相交面积判断窗口归属。启动或显示器配置变化后，FocusVeil 会使用每个显示器上最靠前的可见窗口补足初始焦点状态。

### 验证建议

- 连接两块或更多显示器，在不同屏幕上分别打开普通窗口。
- 在一块显示器上切换应用，确认其他显示器的主要窗口不被错误压暗。
- 启用分级亮度，确认历史窗口在每块显示器上独立排序。

## v0.1.3

发布日期：2026 年 7 月 29 日

### 发布摘要

优化分级亮度的默认分布和视觉响应。默认背景压暗强度提高到百分之五十二，历史窗口亮度不再均匀分布，而是让较低层级更快接近背景压暗区域。

### 用户影响

历史窗口与当前活动窗口之间的视觉差异更清楚。用户启用分级亮度后，可以保留最近操作过的上下文，同时减少历史窗口被误认为当前窗口的概率。

### 技术说明

渲染时会对历史层级的保留亮度应用视觉响应曲线，使较低亮度设置对应更明显的压暗效果。已经手动调整过分级亮度的用户，可以在菜单中使用恢复默认来应用新的默认分布。

### 验证建议

- 启用分级亮度并设置多个历史高亮窗口。
- 在多个窗口之间依次切换，确认较近历史窗口比更早历史窗口更亮。
- 使用恢复默认，确认亮度分布回到新版本默认值。

## v0.1.2

发布日期：2026 年 7 月 29 日

### 发布摘要

修复 macOS 点按墙纸以显示桌面后遮罩无法恢复的问题。应用现在会识别 Finder 桌面获得焦点但没有普通窗口的状态，并在桌面动画期间保留最近活动窗口记录。

### 用户影响

用户点击桌面使窗口散开，再次点击恢复窗口后，FocusVeil 会重新把遮罩放回正确层级。遮罩不会因为显示桌面动画中的临时窗口位置而失效。

### 技术说明

本版本改进窗口可见性判断。窗口必须有足够面积位于当前显示器范围内，才会被视为可用于恢复遮罩层级的普通窗口。遮罩窗口也改为瞬时辅助窗口行为，降低被系统显示桌面机制错误处理的概率。

### 验证建议

- 启用系统设置中的点按墙纸以显示桌面功能。
- 打开多个应用窗口并启用 FocusVeil。
- 点击桌面使窗口散开，再次点击恢复窗口。
- 确认 FocusVeil 遮罩恢复正常，当前活动窗口保持原有亮度，其他窗口与桌面背景继续被压暗。

## v0.1.1

发布日期：2026 年 7 月 29 日

### 发布摘要

更新应用版本号，为初始版本后的发布流程做准备。

### 用户影响

应用功能与 v0.1.0 保持一致。该版本主要用于保证构建产物、应用元数据和发布版本号一致。

### 技术说明

更新 `Resources/Info.plist` 中的版本字段。

### 验证建议

- 打开关于窗口，确认显示的版本号为 0.1.1。
- 运行构建脚本，确认生成的应用包可以正常启动。

## v0.1.0

发布日期：2026 年 7 月 29 日

### 发布摘要

发布 FocusVeil 的初始可用版本。应用以原生 macOS 菜单栏形式运行，可以识别当前活动窗口，并压暗其他窗口和桌面背景。

### 用户影响

用户可以在多窗口工作环境中获得更稳定的视觉焦点。应用提供背景压暗强度调节、短时暂停、辅助功能授权入口和登录时启动设置。

### 技术说明

初始版本使用辅助功能接口读取活动窗口边界，并通过全屏透明面板绘制压暗层。本地构建脚本支持编译、临时签名和生成压缩包。

### 验证建议

- 启动 FocusVeil 并授予辅助功能权限。
- 在多个应用窗口之间切换，确认压暗效果跟随当前活动窗口。
- 调整背景压暗强度，确认菜单设置即时生效。
